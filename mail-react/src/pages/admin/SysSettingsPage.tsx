import SysSettings from '../../screens/admin/SysSettings'

export default function SysSettingsPage() {
  return (
    <div className="rounded-lg shadow-1 overflow-hidden" style={{ background: 'var(--surface)' }}>
      <SysSettings />
    </div>
  )
}
